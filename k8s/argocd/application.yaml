apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: subscribe-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/GoormPlay/subscribe-service.git
    targetRevision: deploy
    path: k8s/helm
    helm:
      valueFiles:
        - values.yaml
    submodules: true
  destination:
    server: https://kubernetes.default.svc
    namespace: subscribe
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
